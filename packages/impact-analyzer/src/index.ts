// @pe/impact-analyzer — all change-intelligence lives here (M3: ImpactAnalyzer).
// M2 ships the DiagnosticCache: the staleness oracle the analyzer will use.

export { DiagnosticCache, DEFAULT_TTL_MS } from './diagnostic-cache.js';
export type { DiagnosticCacheOptions, ScanMetadata } from './diagnostic-cache.js';
